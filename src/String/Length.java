package String;

public class Length {

    public static void main(String[] args) {

        String s = "Java";
        String V = "";
        System.out.println(s.length()); //length will always starts from 1
        System.out.println(V.length());
        // Length will also have zero when there is no content String s ="";
    }
}
